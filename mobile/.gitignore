node_modules/
.expo/
dist/
web-build/
*.log
*.tsbuildinfo
.DS_Store
.env*
!.env.example
.codex-remote-attachments/
Tasks.docx
build command.txt
ios/
android/
*.tgz
*.jks
*.p8
*.p12
credentials.json
key.properties

# @generated expo-cli sync-2b81b286409207a5da26e14c78851eb30d8ccbdb
# The following patterns were generated by expo-cli

expo-env.d.ts
# @end expo-cli

# Face AR SDK build artifacts — bundle (.txt) is committed so EAS/Metro finds
# it without a prebuild step; the dev sourcemap + meta are regenerated by
# `npm run facear:build` and are not tracked.
assets/facear/*.map
assets/facear/*.meta.json
